{% comment %}
  Renders model details from product.metafields.custom.models_details
{% endcomment %}

{%- liquid
  assign model = product.metafields.custom.models_details.value

  assign model_name = blank
  if model != blank
    assign model_name = model.model_name.value | default: model.model_name
    if model_name == blank
      assign model_name = model.display_name.value | default: model.display_name
    endif
    if model_name == blank
      assign model_name = model.name.value | default: model.name
    endif
  endif
-%}

{% if model != blank %}
  <div class="model-details" style="padding: 16px 0;">
    <div style="display: flex; align-items: center; gap: 24px;">
      {% if model.model_image.value != blank %}
        <img
          src="{{ model.model_image.value | image_url: width: 120 }}"
          alt="{{ model_name | default: 'Model' | escape }}"
          width="80"
          height="80"
          loading="lazy"
          style="width: 80px; height: 80px; border-radius: 50%; object-fit: cover; flex-shrink: 0;"
        >
      {% endif %}

      <div>
        {% if model_name != blank %}
          <p style="font-weight: 900; font-style: italic; margin: 0 0 12px; font-size: 15px; text-transform: uppercase; letter-spacing: 0.05em;">
            {{ model_name }}
          </p>
        {% endif %}

        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 0px 40px; font-size: 13px; letter-spacing: 0.03em;">
          {% if model.model_height.value != blank or model.model_height != blank %}
            <span><strong>Athlete HEIGHT:</strong> {{ model.model_height.value | default: model.model_height }}</span>
          {% endif %}
          {% if model.athlete_weight.value != blank or model.athlete_weight != blank %}
            <span><strong>Athlete Weight:</strong> {{ model.athlete_weight.value | default: model.athlete_weight }}</span>
          {% endif %}
          {% if model.model_waist.value != blank or model.model_waist != blank %}
            <span><strong>WAIST:</strong> {{ model.model_waist.value | default: model.model_waist }}</span>
          {% endif %}
          {% if model.wears.value != blank or model.wears != blank %}
            <span><strong>WEARS:</strong> {{ model.wears.value | default: model.wears }}</span>
          {% endif %}
        </div>
      </div>
    </div>
  </div>
{% endif %}
